<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CarbonTest" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/app_purple</item>
        <item name="colorPrimaryVariant">@color/app_purple</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/dark</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ToolBarBackground">
        <item name="contentScrim">@color/app_purple</item>
    </style>

    <style name="ActionBarBackground">
        <item name="android:background">@color/app_purple</item>
    </style>

    <style name="MovieBackground">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="MovieCard">
        <item name="android:background">@drawable/layout_rounded_background</item>
        <item name="android:backgroundTint">@color/gray</item>
    </style>

    <style name="MovieTitle">
        <item name="android:textColor">@color/dark</item>
    </style>

    <style name="MovieSubtitle">
        <item name="android:textColor">@color/dark_text</item>
    </style>

</resources>